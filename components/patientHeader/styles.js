import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  header: {
    backgroundColor: '#0f3995',
    flexDirection: 'row',
    alignItems: 'center',
    padding: 10,
  },

  pName: {
    fontSize: 20,
    fontWeight: '500',
    color: '#fff',
  },
  pAge: {
    color: '#fff',
  },
});

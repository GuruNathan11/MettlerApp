import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  btnView: {
    maxHeight: 70,
    padding: '5%',
    alignItems: 'center',
    justifyContent: 'center',
    width: '80%',
    borderRadius: 6,
    marginTop: '4%',
  },

  btnText: {
    color: '#fff',
    fontWeight: '900',
    fontSize: 15,
  },
});

const {StyleSheet} = require('react-native');

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    backgroundColor: '#fff',
    width: 150,
    height: 60,
    gap: 10,
    alignItems: 'center',
    borderWidth: 0.5,
    borderColor: '#ccc',
    margin: 5,
    borderRadius: 10,
  },
});
